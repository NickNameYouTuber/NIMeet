# Быстрый старт NIMeet

## 🐳 Способ 1: Docker Compose (Рекомендуется)

### Самый простой способ - одна команда!

```bash
docker-compose up
```

Готово! Приложение доступно на:
- **Frontend**: http://localhost:3000
- **Backend**: http://localhost:5000
- **MongoDB**: автоматически настроен

Для фонового режима:
```bash
docker-compose up -d
```

Остановка:
```bash
docker-compose down
```

📚 **Подробная документация**: [DOCKER.md](DOCKER.md)

---

## 💻 Способ 2: Локальная установка

### Шаг 1: Запуск MongoDB

Если у вас не установлен MongoDB, установите его или используйте MongoDB Atlas (облачный).

**Локальный MongoDB:**
```bash
mongod
```

**MongoDB Atlas:**
1. Создайте бесплатный кластер на https://www.mongodb.com/cloud/atlas
2. Получите connection string
3. Используйте его в `.env` файле backend

## Шаг 2: Настройка Backend

```bash
cd backend

npm install

echo "PORT=5000
MONGODB_URI=mongodb://localhost:27017/nimeet
JWT_SECRET=supersecretkey123
CLIENT_URL=http://localhost:3000
NODE_ENV=development" > .env

npm run dev
```

Backend запустится на http://localhost:5000

## Шаг 3: Настройка Frontend

Откройте новый терминал:

```bash
cd frontend

npm install

echo "REACT_APP_API_URL=http://localhost:5000
REACT_APP_SOCKET_URL=http://localhost:5000" > .env

npm start
```

Frontend откроется на http://localhost:3000

## Шаг 4: Тестирование

1. Откройте http://localhost:3000
2. Зарегистрируйте нового пользователя
3. Создайте комнату
4. Откройте второе окно браузера (или в режиме инкогнито)
5. Зарегистрируйте второго пользователя
6. Присоединитесь к комнате используя ID из первого окна
7. Наслаждайтесь видеозвонком! 🎉

## Решение проблем

### MongoDB не подключается
- Убедитесь что MongoDB запущен: `mongod`
- Проверьте правильность `MONGODB_URI` в `.env`

### Backend не запускается
- Проверьте что порт 5000 свободен
- Убедитесь что все зависимости установлены: `npm install`
- Проверьте `.env` файл

### Frontend не подключается к Backend
- Убедитесь что backend запущен на порту 5000
- Проверьте `.env` файл в frontend
- Откройте консоль браузера для ошибок

### Видео не отображается
- Разрешите доступ к камере и микрофону в браузере
- Убедитесь что используете HTTPS (или localhost)
- Проверьте консоль браузера для ошибок WebRTC

### CORS ошибки
- Убедитесь что `CLIENT_URL` в backend `.env` соответствует URL frontend
- Проверьте что оба сервера запущены

## Команды npm

### Backend
- `npm start` - запуск в обычном режиме
- `npm run dev` - запуск с nodemon (auto-reload)

### Frontend
- `npm start` - запуск development сервера
- `npm run build` - создание production build
- `npm test` - запуск тестов

## Порты по умолчанию

- Frontend: 3000
- Backend: 5000
- MongoDB: 27017

## 🚀 Рекомендации по развертыванию

### Development
Используйте Docker Compose для удобства:
```bash
docker-compose up -d
```

### Production
Используйте production конфигурацию:
```bash
docker-compose -f docker-compose.prod.yml up -d
```

## Следующие шаги

- 🐳 Изучите [DOCKER.md](DOCKER.md) для Docker развертывания
- 📖 Изучите [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md) для понимания архитектуры
- 📚 Прочитайте [frontend/README.md](frontend/README.md) и [backend/README.md](backend/README.md)
- 🌐 Настройте production деплой
- 🔄 Добавьте TURN сервер для работы через NAT
- 🎨 Кастомизируйте UI под свои нужды

## Поддержка

Если возникли проблемы:
1. Проверьте логи в терминале backend
2. Откройте консоль разработчика в браузере (F12)
3. Убедитесь что все зависимости установлены
4. Проверьте версию Node.js (требуется 16+)

Удачи! 🚀

