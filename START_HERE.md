# 🚀 Запуск NIMeet

## Самый простой способ запуска

### ⚡ Одна команда

```bash
docker-compose up
```

### ✅ Что произойдет

1. MongoDB запустится на порту 27017
2. Backend запустится на порту 5000
3. Frontend запустится на порту 3000

### 🌐 Откройте браузер

```
http://localhost:3000
```

---

## 📋 Пошаговая инструкция

### Шаг 1: Убедитесь что Docker установлен

```bash
docker --version
docker-compose --version
```

Если нет Docker:
- Windows/Mac: [Docker Desktop](https://www.docker.com/products/docker-desktop)
- Linux: [Docker Engine](https://docs.docker.com/engine/install/)

### Шаг 2: Запустите проект

```bash
docker-compose up
```

При первом запуске:
- Скачаются образы (MongoDB, Node)
- Установятся зависимости
- Запустятся все сервисы

Это займет 2-3 минуты.

### Шаг 3: Дождитесь сообщений

В терминале вы увидите:
```
nimeet-mongodb | MongoDB готов к подключениям
nimeet-backend | Сервер запущен на порту 5000
nimeet-frontend | Compiled successfully!
```

### Шаг 4: Откройте приложение

Перейдите на http://localhost:3000

---

## 🎯 Первое использование

### 1. Регистрация

- Нажмите "Зарегистрироваться"
- Введите:
  - Имя пользователя
  - Email
  - Пароль (минимум 6 символов)

### 2. Создание звонка

- После входа вы попадете на Dashboard
- Нажмите **"Создать комнату и присоединиться"**
- Разрешите доступ к камере и микрофону

### 3. Приглашение участников

- Скопируйте ссылку на комнату (кнопка копирования)
- Отправьте ссылку другим участникам

**Для тестирования:**
- Откройте второе окно браузера (Ctrl+N)
- Перейдите в режим инкогнито (Ctrl+Shift+N)
- Зарегистрируйте второго пользователя
- Вставьте скопированную ссылку

### 4. Управление звонком

В нижней панели:
- 🎤 **Микрофон** - включить/выключить
- 📹 **Камера** - включить/выключить
- 🖥️ **Экран** - демонстрация экрана
- 📞 **Красная кнопка** - завершить звонок

---

## 🔧 Полезные команды

### Запуск в фоновом режиме

```bash
docker-compose up -d
```

### Просмотр логов

```bash
docker-compose logs -f
```

Логи конкретного сервиса:
```bash
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f mongodb
```

### Остановка

```bash
docker-compose down
```

Остановка с очисткой данных:
```bash
docker-compose down -v
```

⚠️ Это удалит всех пользователей и комнаты из БД!

### Перезапуск

```bash
docker-compose restart
```

Перезапуск конкретного сервиса:
```bash
docker-compose restart backend
```

### Пересборка

Если изменили код:
```bash
docker-compose up --build
```

### Проверка статуса

```bash
docker-compose ps
```

---

## 🐛 Решение проблем

### Порт уже занят

**Проблема:** `Error: Port 3000 is already in use`

**Решение:**
1. Остановите другие приложения на портах 3000, 5000, 27017
2. Или измените порты в `docker-compose.yml`

### Docker daemon не запущен

**Проблема:** `Cannot connect to Docker daemon`

**Решение:**
- Windows/Mac: Запустите Docker Desktop
- Linux: `sudo systemctl start docker`

### Недостаточно памяти

**Проблема:** Контейнеры падают из-за памяти

**Решение:**
- Docker Desktop → Settings → Resources
- Увеличьте Memory до 4GB

### Не работает hot reload

**Проблема:** Изменения кода не применяются

**Решение:**
```bash
docker-compose down
docker-compose up --build
```

### База данных пустая после перезапуска

**Проблема:** Пользователи пропали

**Причина:** Запустили `docker-compose down -v` (удаляет volumes)

**Решение:** Используйте просто `docker-compose down`

---

## 📚 Дополнительная информация

### Детальная документация

- [DOCKER.md](DOCKER.md) - Полное руководство по Docker
- [QUICKSTART.md](QUICKSTART.md) - Локальная установка без Docker
- [README.md](README.md) - Общая документация проекта
- [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md) - Архитектура

### Production развертывание

Для production используйте:
```bash
docker-compose -f docker-compose.prod.yml up -d
```

Подробнее в [DOCKER.md](DOCKER.md#production-развертывание)

### Структура проекта

Смотрите [FILES_CREATED.md](FILES_CREATED.md) - полный список файлов

---

## ✅ Checklist первого запуска

- [ ] Docker установлен и запущен
- [ ] Выполнена команда `docker-compose up`
- [ ] Открыт http://localhost:3000
- [ ] Создан первый пользователь
- [ ] Создана первая комната
- [ ] Разрешен доступ к камере/микрофону
- [ ] Видео работает! 🎉

---

## 💡 Советы

### Для разработки

1. Используйте VSCode с расширениями:
   - Docker
   - ESLint
   - Tailwind CSS IntelliSense

2. Логи в режиме реального времени:
   ```bash
   docker-compose logs -f
   ```

3. Вход в контейнер для отладки:
   ```bash
   docker-compose exec backend sh
   docker-compose exec frontend sh
   ```

### Для production

1. Обязательно измените:
   - JWT_SECRET
   - Пароли MongoDB
   - CLIENT_URL

2. Настройте HTTPS (Let's Encrypt)

3. Добавьте TURN сервер для работы через NAT

4. Настройте backups MongoDB

---

## 🎉 Готово!

Ваше приложение для видеозвонов запущено!

**Основные возможности:**
✅ Регистрация и вход  
✅ Создание комнат  
✅ Видео/аудио связь  
✅ Демонстрация экрана  
✅ Управление камерой и микрофоном  

**Следующие шаги:**
- Пригласите друзей для тестирования
- Изучите код в `frontend/src` и `backend/src`
- Кастомизируйте под свои нужды
- Разверните в production

---

## 📞 Поддержка

**Документация:**
- [DOCKER.md](DOCKER.md) - Docker руководство
- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт
- [README.md](README.md) - Главная документация

**Проверьте:**
- Логи: `docker-compose logs -f`
- Статус: `docker-compose ps`
- Порты: 3000, 5000, 27017

---

**Удачи! 🚀**

*NIMeet - современное приложение для видеозвонов*

